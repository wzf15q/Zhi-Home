---
import BaseLayout from '../layouts/BaseLayout.astro';

// AI Tools Data
const aiTools = [
  {
    name: "ChatGPT",
    description: "OpenAI 开发的强大对话式 AI 助手，支持多种任务",
    url: "https://chat.openai.com",
    category: "chat",
    tags: ["对话", "写作", "编程"],
    icon: "💬",
    featured: true
  },
  {
    name: "Claude",
    description: "Anthropic 的 AI 助手，擅长长文本理解和分析",
    url: "https://claude.ai",
    category: "chat",
    tags: ["对话", "分析", "写作"],
    icon: "🤖",
    featured: true
  },
  {
    name: "Midjourney",
    description: "顶级 AI 图像生成工具，创造惊艳的艺术作品",
    url: "https://www.midjourney.com",
    category: "image",
    tags: ["图像", "艺术", "设计"],
    icon: "🎨",
    featured: true
  },
  {
    name: "DALL·E 3",
    description: "OpenAI 的图像生成模型，精准理解文字描述",
    url: "https://openai.com/dall-e-3",
    category: "image",
    tags: ["图像", "创意", "设计"],
    icon: "🖼️"
  },
  {
    name: "GitHub Copilot",
    description: "AI 编程助手，实时代码建议和自动补全",
    url: "https://github.com/features/copilot",
    category: "coding",
    tags: ["编程", "开发", "效率"],
    icon: "💻",
    featured: true
  },
  {
    name: "Cursor",
    description: "AI 驱动的代码编辑器，智能编程体验",
    url: "https://cursor.sh",
    category: "coding",
    tags: ["编程", "IDE", "AI"],
    icon: "⚡"
  },
  {
    name: "Notion AI",
    description: "集成在 Notion 中的 AI 写作和整理助手",
    url: "https://www.notion.so/product/ai",
    category: "writing",
    tags: ["写作", "笔记", "整理"],
    icon: "✍️"
  },
  {
    name: "Grammarly",
    description: "AI 写作助手，语法检查和文风优化",
    url: "https://www.grammarly.com",
    category: "writing",
    tags: ["写作", "语法", "优化"],
    icon: "📝"
  },
  {
    name: "Runway",
    description: "AI 视频编辑和生成平台，创意无限",
    url: "https://runwayml.com",
    category: "video",
    tags: ["视频", "编辑", "创意"],
    icon: "🎬"
  },
  {
    name: "ElevenLabs",
    description: "AI 语音合成工具，生成自然流畅的语音",
    url: "https://elevenlabs.io",
    category: "audio",
    tags: ["语音", "音频", "合成"],
    icon: "🎙️"
  },
  {
    name: "Perplexity",
    description: "AI 搜索引擎，智能问答和信息检索",
    url: "https://www.perplexity.ai",
    category: "utility",
    tags: ["搜索", "问答", "研究"],
    icon: "🔍"
  },
  {
    name: "Jasper",
    description: "AI 营销文案生成工具，提升内容创作效率",
    url: "https://www.jasper.ai",
    category: "writing",
    tags: ["营销", "文案", "创作"],
    icon: "📢"
  }
];

const categories = [
  { id: "all", name: "全部", icon: "🌟" },
  { id: "chat", name: "对话助手", icon: "💬" },
  { id: "image", name: "图像生成", icon: "🎨" },
  { id: "coding", name: "编程助手", icon: "💻" },
  { id: "writing", name: "写作工具", icon: "✍️" },
  { id: "video", name: "视频工具", icon: "🎬" },
  { id: "audio", name: "音频工具", icon: "🎙️" },
  { id: "utility", name: "实用工具", icon: "🔧" }
];
---

<BaseLayout 
  title="Zhi Home - AI 工具导航 | 精选 AI 工具集合"
  description="发现最优秀的 AI 工具，提升工作效率和创造力"
>
  <!-- Navigation -->
  <nav class="navbar">
    <div class="container navbar-content">
      <div class="brand">Zhi Home</div>
      <div class="nav-actions">
        <button class="btn btn-secondary btn-icon" id="themeToggle" aria-label="切换主题">
          <span id="themeIcon">🌙</span>
        </button>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <img src={`${import.meta.env.BASE_URL}avatar.svg`} alt="王ZZ的头像" class="hero-avatar" />
      <h1 class="hero-title">你好，我是王ZZ 👋</h1>
      <p class="hero-subtitle">
        前大厂边缘运营 · 现纯小白初学者 · 终身学习爱好者
      </p>
      <div class="hero-bio">
        <p>
          曾在大厂做运营，主要负责边缘业务（就是那种领导开会都想不起来的部门😅）。
          某天突然顿悟：与其在边缘徘徊，不如从零开始学点真本事！
        </p>
        <p>
          于是乎，一个对代码一窍不通的运营人，开始了自学编程之路。
          现在的我：能看懂报错信息就算进步，能跑通 Hello World 就要庆祝一番 🎉
        </p>
        <p>
          这个网站是我学习路上的小作品，收集了一些我觉得超酷的 AI 工具。
          如果你也是小白，咱们一起加油！如果你是大神，请轻拍 🙏
        </p>
      </div>
      <div class="hero-stats">
        <div class="stat-item">
          <span class="stat-icon">☕</span>
          <span class="stat-label">咖啡续命</span>
        </div>
        <div class="stat-item">
          <span class="stat-icon">🐛</span>
          <span class="stat-label">Debug 日常</span>
        </div>
        <div class="stat-item">
          <span class="stat-icon">💡</span>
          <span class="stat-label">灵光一现</span>
        </div>
        <div class="stat-item">
          <span class="stat-icon">🚀</span>
          <span class="stat-label">持续进化</span>
        </div>
      </div>
      <div class="hero-links">
        <a href="https://github.com" class="social-link" target="_blank" rel="noopener">
          <span>🔗</span>
          <span>GitHub</span>
        </a>
        <a href="https://twitter.com" class="social-link" target="_blank" rel="noopener">
          <span>🐦</span>
          <span>Twitter</span>
        </a>
        <a href="mailto:hello@example.com" class="social-link">
          <span>✉️</span>
          <span>联系我</span>
        </a>
      </div>
    </div>
  </section>

  <!-- Tools Section -->
  <section class="section">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">AI 工具导航</h2>
        <p class="section-description">探索 {aiTools.length} 个精选 AI 工具，涵盖对话、图像、编程、写作等多个领域</p>
      </div>

      <!-- Search Bar -->
      <div class="search-bar">
        <input 
          type="text" 
          class="search-input" 
          id="searchInput"
          placeholder="搜索工具名称、描述或标签..." 
          aria-label="搜索工具"
        />
        <span class="search-icon">🔍</span>
      </div>

      <!-- Category Filters -->
      <div class="filter-tabs">
        {categories.map(cat => (
          <button 
            class={`filter-tab ${cat.id === 'all' ? 'active' : ''}`}
            data-category={cat.id}
          >
            <span>{cat.icon}</span>
            <span>{cat.name}</span>
          </button>
        ))}
      </div>

      <!-- Tools Grid -->
      <div class="tools-grid fade-in" id="toolsGrid">
        {aiTools.map(tool => (
          <a 
            href={tool.url} 
            target="_blank" 
            rel="noopener noreferrer"
            class="tool-card"
            data-category={tool.category}
            data-tags={tool.tags.join(',')}
            data-name={tool.name.toLowerCase()}
            data-description={tool.description.toLowerCase()}
          >
            {tool.featured && <span class="tool-badge">精选</span>}
            <div class="tool-icon">{tool.icon}</div>
            <h3 class="tool-name">{tool.name}</h3>
            <p class="tool-description">{tool.description}</p>
            <div class="tool-tags">
              {tool.tags.map(tag => (
                <span class="tag">{tag}</span>
              ))}
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <p class="footer-text">© 2026 Zhi Home | 精选 AI 工具导航 | 持续更新中</p>
    </div>
  </footer>

  <script>
    // Theme Toggle
    const themeToggle = document.getElementById('themeToggle');
    const themeIcon = document.getElementById('themeIcon');
    const html = document.documentElement;
    
    // Initialize theme from localStorage
    const savedTheme = localStorage.getItem('theme') || 'light';
    if (savedTheme === 'dark') {
      html.setAttribute('data-theme', 'dark');
      themeIcon.textContent = '☀️';
    }
    
    themeToggle.addEventListener('click', () => {
      const currentTheme = html.getAttribute('data-theme');
      const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
      
      // Add transition class
      document.body.classList.add('theme-transition');
      
      html.setAttribute('data-theme', newTheme);
      localStorage.setItem('theme', newTheme);
      themeIcon.textContent = newTheme === 'dark' ? '☀️' : '🌙';
      
      // Remove transition class after animation
      setTimeout(() => {
        document.body.classList.remove('theme-transition');
      }, 350);
    });

    // Search and Filter Functionality
    const searchInput = document.getElementById('searchInput');
    const filterTabs = document.querySelectorAll('.filter-tab');
    const toolCards = document.querySelectorAll('.tool-card');
    
    let currentCategory = 'all';
    let searchTerm = '';
    
    // Filter function
    function filterTools() {
      toolCards.forEach(card => {
        const category = card.getAttribute('data-category');
        const name = card.getAttribute('data-name');
        const description = card.getAttribute('data-description');
        const tags = card.getAttribute('data-tags').toLowerCase();
        
        const matchesCategory = currentCategory === 'all' || category === currentCategory;
        const matchesSearch = searchTerm === '' || 
                             name.includes(searchTerm) || 
                             description.includes(searchTerm) ||
                             tags.includes(searchTerm);
        
        if (matchesCategory && matchesSearch) {
          card.style.display = 'block';
          // Trigger reflow for animation
          setTimeout(() => {
            card.style.opacity = '1';
            card.style.transform = 'translateY(0)';
          }, 10);
        } else {
          card.style.opacity = '0';
          card.style.transform = 'translateY(20px)';
          setTimeout(() => {
            card.style.display = 'none';
          }, 250);
        }
      });
    }
    
    // Search input handler
    searchInput.addEventListener('input', (e) => {
      searchTerm = e.target.value.toLowerCase().trim();
      filterTools();
    });
    
    // Category filter handler
    filterTabs.forEach(tab => {
      tab.addEventListener('click', () => {
        // Update active state
        filterTabs.forEach(t => t.classList.remove('active'));
        tab.classList.add('active');
        
        // Update current category
        currentCategory = tab.getAttribute('data-category');
        filterTools();
      });
    });
    
    // Prevent default link behavior during filter animations
    toolCards.forEach(card => {
      card.style.transition = 'all 0.25s cubic-bezier(0.4, 0, 0.2, 1)';
    });
  </script>
</BaseLayout>